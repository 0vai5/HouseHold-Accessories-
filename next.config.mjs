/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'encrypted-tbn0.gstatic.com', 'd1iv6qgcmtzm6l.cloudfront.net', 'tailwindui.com'
        ],
    },
};

export default nextConfig;
